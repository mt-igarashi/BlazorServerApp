SgnalRのクラスが存在しない場合は以下をインストール
dotnet add package Microsoft.AspNetCore.SignalR.Client --version 5.0.11

まだデバッグは実行できないので
ターミナルで dotnet watch run を実行するか
デバッグなしで実行する。

Visual Studio CodeにBlazorのテンプレートがない場合は
.templateengine\dotnetcliフォルダは以下のファイルを消去する。

ChatはタブでChat画面をもう一つ開けば通信ができているのを確認できる。